-k = use kernel addresses
-u = use userspace addresses
-c NAME

--run-for=1hour
--numcalls=1000000
--silent
--logfile

--nrsyscalls (set NR_SYSCALLS)

flush() & fsync() before making the call

use syscall()

8 args ?

skip select() sigsuspend() and pause()

